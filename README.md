# H&Y Warehouse Management System
### سیستم جامع مدیریت انبارداری قطعات الکترونیک و تولید

![Project Status](https://img.shields.io/badge/Status-Active-success)
![Version](https://img.shields.io/badge/Version-0.23-blue)
![Python](https://img.shields.io/badge/Backend-Python%20Flask-yellow)
![React](https://img.shields.io/badge/Frontend-React%20%7C%20Tailwind-blueviolet)
![MySQL](https://img.shields.io/badge/Database-MySQL-orange)

## 📋 معرفی پروژه (Overview)
این پروژه یک سیستم نرم‌افزاری پیشرفته برای **مدیریت موجودی انبار قطعات الکترونیک**، **مدیریت پروژه‌های تولیدی (BOM)** و **حسابداری قیمت تمام شده** است. این نرم‌افزار به گونه‌ای طراحی شده که فرآیند ورود و خروج قطعات، محاسبه کسری‌های تولید و مدیریت کاربران را به صورت متمرکز و با رابط کاربری مدرن (SPA) انجام دهد.

معماری پروژه بر اساس **Python Flask** در سمت سرور و **React.js** (به صورت ماژولار و بدون بیلد سیستم پیچیده) در سمت کاربر طراحی شده است و از دیتابیس **MySQL** برای ذخیره‌سازی داده‌ها استفاده می‌کند.

---

## ✨ ویژگی‌های کلیدی (Key Features)

### 📦 مدیریت انبار (Inventory Management)
* **ورود و خروج کالا:** ثبت دقیق قطعات با مشخصات فنی (پکیج، تولرانس، توان و ...).
* **دسته‌بندی پویا:** پشتیبانی از انواع قطعات (مقاومت، خازن، IC، کانکتور و ...).
* **جستجوی پیشرفته:** فیلترینگ بر اساس نام، مقدار، لوکیشن و کد انبار.
* **کدینگ خودکار:** تولید شماره فنی (Part Code) منحصر‌به‌فرد برای هر قطعه.

### 🛠 مدیریت پروژه‌ها و BOM (Projects & BOM)
* **تعریف پروژه:** ایجاد پروژه جدید با قابلیت نسخه برداری (Duplicate).
* **لیست قطعات (BOM):** افزودن قطعات به پروژه و محاسبه خودکار قیمت تمام شده.
* **محاسبه کسری:** بررسی هوشمند موجودی انبار برای تولید تعداد مشخصی از یک پروژه.
* **کسر گروهی:** قابلیت کسر اتوماتیک تمام قطعات یک پروژه از انبار با یک کلیک.
* **مدیریت هزینه‌ها:** افزودن هزینه‌های جانبی (مونتاژ، PCB و ...) به پروژه.

### 💰 مدیریت مالی و ارز
* **قیمت لحظه‌ای دلار:** دریافت اتوماتیک نرخ ارز از وب‌سایت‌های مرجع (TGJU) و بروزرسانی ارزش انبار.
* **محاسبه ارزش انبار:** گزارش مجموع ریالی و دلاری موجودی انبار.

### 🛡 امنیت و مدیریت (Admin & Security)
* **سطوح دسترسی:** سیستم Role-based (ادمین و اپراتور) با قابلیت تعریف دسترسی ریز (Granular Permissions).
* **لاگ تغییرات:** ثبت دقیق تمام تراکنش‌ها (چه کسی، چه زمانی، چه تغییری و به چه دلیلی).
* **پشتیبان‌گیری:** سیستم بک‌آپ‌گیری و بازگردانی دیتابیس (SQL Dump) از داخل پنل ادمین.
* **سرویس Watchdog:** نظارت بر فعالیت کلاینت و مدیریت خاموش شدن سرور.

---

## 🛠 تکنولوژی‌های استفاده شده (Tech Stack)

### Backend (Server-side)
* **Language:** Python 3.x
* **Framework:** Flask
* **Database Driver:** MySQL Connector
* **Utilities:** Threading (Watchdog), Requests (Scraping), Subprocess (Backup)

### Frontend (Client-side)
* **Library:** React.js 18 (UMD/Standalone)
* **Styling:** Tailwind CSS (Custom Config)
* **UI Components:** Lucide Icons, Framer Motion, SweetAlert2
* **HTTP Client:** Axios

### Database
* **MySQL 8.0+**

---

## 🚀 راهنمای نصب و اجرا (Installation)

### پیش‌نیازها
1.  نصب **Python 3.10** یا بالاتر.
2.  نصب **MySQL Server**.

### مراحل نصب
1.  **کلون کردن مخزن:**
    ```bash
    git clone [https://github.com/your-username/repository-name.git](https://github.com/your-username/repository-name.git)
    cd repository-name
    ```

2.  **نصب کتابخانه‌های پایتون:**
    ```bash
    pip install flask flask-cors mysql-connector-python requests beautifulsoup4
    ```

3.  **پیکربندی دیتابیس:**
    * یک دیتابیس خالی با نام `HY` در MySQL ایجاد کنید.
    * فایل `server_config.json` را باز کرده و اطلاعات اتصال (User, Password) را تنظیم کنید.

4.  **اجرای برنامه:**
    ```bash
    python warehouse_app.py
    ```
    * برنامه به صورت خودکار جداول مورد نیاز را در دیتابیس می‌سازد (`init_db`).
    * مرورگر شما باز شده و برنامه در آدرس `http://127.0.0.1:8090` اجرا می‌شود.

---

## 📂 ساختار پروژه (Project Structure)

```plaintext
H&Y-Warehouse/
│
├── warehouse_app.py      # نقطه شروع برنامه (Entry Point)
├── config.py             # تنظیمات و ثابت‌های سیستم
├── database.py           # مدیریت اتصال دیتابیس و ایجاد جداول
├── routes.py             # API Endpoints و منطق سرور
├── services.py           # سرویس‌های جانبی (قیمت دلار، Watchdog)
├── auth_utils.py         # توابع امنیتی و هش پسورد
│
├── static/               # فایل‌های استاتیک فرانت‌اند
│   ├── js/               # کدهای جاوااسکریپت (React Components)
│   │   ├── Main Application.js
│   │   ├── UI Components.js
│   │   ├── Dashboard Page.js
│   │   ├── Projects Page.js
│   │   └── ...
│   ├── libs/             # کتابخانه‌های آفلاین (React, Tailwind, etc.)
│   ├── fonts/            # فونت وزیرمتن (Vazirmatn)
│   └── logo.png
│
├── backups/              # محل ذخیره فایل‌های پشتیبان SQL
├── uploads/              # محل آپلود فایل‌ها (در صورت نیاز)
├── index.html            # فایل اصلی HTML
└── server_config.json    # تنظیمات اتصال به دیتابیس